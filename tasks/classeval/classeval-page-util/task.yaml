name: classeval-page-util
language: python
difficulty: easy
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `PageUtil` class in `src/page_util.py`.
  
  PageUtil class is a versatile utility for handling pagination and search functionalities in an efficient and convenient manner.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
