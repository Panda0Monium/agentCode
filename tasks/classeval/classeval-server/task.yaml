name: classeval-server
language: python
difficulty: hard
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `Server` class in `src/server.py`.
  
  This is a class as a server, which handles a white list, message sending and receiving, and information display.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
