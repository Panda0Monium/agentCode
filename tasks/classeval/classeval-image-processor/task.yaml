name: classeval-image-processor
language: python
difficulty: easy
timeout_sec: 300
docker_image: agentcode-classeval

instruction: |
  Implement the `ImageProcessor` class in `src/image_processor.py`.
  
  This is a class to process image, including loading, saving, resizing, rotating, and adjusting the brightness of images.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
