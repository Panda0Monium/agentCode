name: classeval-argument-parser
language: python
difficulty: hard
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `ArgumentParser` class in `src/argument_parser.py`.
  
  This is a class for parsing command line arguments to a dictionary.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
