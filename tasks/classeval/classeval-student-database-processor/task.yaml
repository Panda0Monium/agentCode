name: classeval-student-database-processor
language: python
difficulty: medium
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `StudentDatabaseProcessor` class in `src/student_database_processor.py`.
  
  This is a class with database operation, including inserting student information, searching for student information by name, and deleting student information by name.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
