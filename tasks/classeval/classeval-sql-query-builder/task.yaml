name: classeval-sql-query-builder
language: python
difficulty: easy
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `SQLQueryBuilder` class in `src/sql_query_builder.py`.
  
  This class provides to build SQL queries, including SELECT, INSERT, UPDATE, and DELETE statements.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
