name: classeval-balanced-brackets
language: python
difficulty: hard
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `BalancedBrackets` class in `src/balanced_brackets.py`.
  
  This is a class that checks for bracket matching
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
