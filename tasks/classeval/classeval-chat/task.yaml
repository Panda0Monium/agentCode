name: classeval-chat
language: python
difficulty: medium
timeout_sec: 300
docker_image: agentcode-base

instruction: |
  Implement the `Chat` class in `src/chat.py`.
  
  This is a chat class with the functions of adding users, removing users, sending messages, and obtaining messages.
  
  The class skeleton with all method signatures and docstrings is already in place.

grader_weights:
  public: 0.3
  private: 0.6
  lint: 0.1
